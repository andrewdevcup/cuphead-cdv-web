return function(thisPlayer, flags, world, scene, game, charmData) {
	//Setup actions
	flags.heartPoints+=2;
	
	thisPlayer.data.damageMultiplier *= 0.9;
	
	return {
		onTick: function() {
			//Upddate
		}
	};
};