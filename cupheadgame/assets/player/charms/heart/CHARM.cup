return function(thisPlayer, flags, world, scene, game, charmData) {
	//Setup actions
	flags.heartPoints++;
	
	thisPlayer.data.damageMultiplier *= 0.95;
	
	return {
		onTick: function() {
			//Upddate
		}
	};
};