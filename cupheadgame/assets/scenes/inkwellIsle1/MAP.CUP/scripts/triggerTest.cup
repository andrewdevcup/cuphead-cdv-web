return function(thisNpc, world, scene, game, data) {
  //On Interact
  //Because NPC is flagged as trigger, this will be called everytime a player enters and exits the hitbox
  
  if(thisNpc.player_focus) {
  	//Player in
  	console.log('Player has entered trigger area of '+ 
  	thisNpc.name + ': '+ thisNpc.player);
  }
  else {
  	//Player out
  	console.log('Player has exited trigger area: '+thisNpc.player)
  }
};